jobs:
  include:
    - arch: ppc64le
      dist: bionic
    - arch: ppc64le
      dist: focal

# arch:
#   - ppc64le

script:
  - cat /etc/os-release
  - cat /proc/cpuinfo
  - cat /proc/meminfo
  - df -h
#  - curl -s https://raw.githubusercontent.com/sivel/speedtest-cli/master/speedtest.py | python -
